# TraLa Configuration File
version: 3.3

services:
  exclude:
    routers:
      - "mariadb"
      - "phpmyadmin"
      - "traefik"
  overrides:
    - service: "mastodon-rss"
      display_name: "Mastodon to RSS"
      group: "RSS"
    - service: "kavita"
      group: "Media"
    - service: "bubble"
      group: "Media"
    - service: "transmission"
      group: "Media"
    - service: "nextcp2"
      group: "Media"